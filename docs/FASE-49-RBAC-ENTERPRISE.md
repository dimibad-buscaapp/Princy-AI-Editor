# FASE 49 — RBAC Enterprise

Policy, CustomRole, Group + `@princy/rbac-kit` evaluator.

## Pacote

`evaluatePolicy({ userId, orgId, action, resource })`

## Modelos

- `Policy`, `CustomRole`, `Group`
